Leetcode 

So we have phases planned 
Phase 1-2: 220 problems (Array + String fundamentals)
Phase 3: 100 problems (LinkedList + Stack/Queue)
Phase 4: 100 problems (HashMap + Binary Search)
Phase 5: 100 problems (Heap + Tree basics)
Phase 6: 80 problems (BST + Graph algorithms)

PHASE 1
Phase 1: Array Patterns (1-120)
A. Array Basics - Pure Easy (1-20)
B. Two Pointers - Gradual Complexity (21-40)
C. Sliding Window - Step by Step (41-70)
D. Prefix Sum & Array Math (71-95)
E. Advanced Array Techniques (96-120)
Phase 2: String Patterns (121-220)
A. String Fundamentals (121-145)
B. Pattern Matching - Easy to Medium (146-170)
C. Advanced String Algorithms (171-195)
D. String DP & Complex Patterns (196-220)
Phase 3: LinkedList Patterns (221-320)
A. LinkedList Fundamentals (221-245)
B. Advanced LinkedList Operations (246-270)
C. Stack Fundamentals (271-295)
D. Queue & Deque Patterns (296-320)
Phase 4: HashMap & Binary Search (321-420)
A. HashMap Fundamentals (321-350)
B. Advanced HashMap Patterns (351-375)
C. Binary Search Fundamentals (376-400)
D. Advanced Binary Search (401-420)
Phase 5: Heap & Tree Fundamentals (421-520)
A. Heap Operations (421-450)
B. Tree Traversals (451-475)
C. Tree Properties & Validation (476-500)
D. Path & Sum Problems (501-520)
Phase 6: BST & Graph Algorithms (521-600)
A. Binary Search Tree Operations (521-545)
B. Graph Traversal & Basic Algorithms (546-570)
C. Advanced Graph Algorithms (571-590)
D. Final Integration & Complex Problems (591-600)















A. Array Basics - Pure Easy (1-20)
1. Running Sum of 1d Array — 1480 (Basic iteration)
2. Find Numbers with Even Number of Digits — 1295 (Digit counting)
3. Max Consecutive Ones — 485 (Simple counting)
4. Squares of a Sorted Array — 977 (Two pointers intro)
5. Duplicate Zeros — 1089 (Array modification)
6. Merge Sorted Array — 88 (Two array merging)
7. Remove Element — 27 (In-place removal)
8. Remove Duplicates from Sorted Array — 26 (Two pointers)
9. Check If N and Its Double Exist — 1346 (HashSet usage)
10. Valid Mountain Array — 941 (Single pass validation) 
11. Replace Elements with Greatest Element on Right Side — 1299 (Right-to-left)
12. Move Zeroes — 283 (Two pointers with zeros)
13. Sort Array By Parity — 905 (Partitioning)
14. Sort Array By Parity II — 922 (Even/odd positioning)
15. Height Checker — 1051 (Comparison with sorted)
16. Third Maximum Number — 414 (Set for top elements)
17. Find All Numbers Disappeared in an Array — 448 (Array as hash)
18. Single Number — 136 (XOR properties) 
19. Majority Element — 169 (Boyer-Moore or counting)
20. Contains Duplicate — 217 (HashSet detection)


Question1. Running Sum of 1d Array — 1480 (Basic iteration)
Question2. Find Numbers with Even Number of Digits — 1295 (Digit counting)
SOLUTION) There could be many ways to do it 
1)String Conversion
String.valueOf(int i) actually calls Integer.toString(i) behind the scenes
int count=0;
       for(int i=0; i<nums.length;i++){
           if(String.valueOf(nums[i]).length()%2==0){
               count++;
2) Counting Digits
By counting digits until the quotient of that number is not equal to zero  
single digit/10 ==0

3)Using Math (log)
Number of digits = floor(log₁₀(number)) + 1       just think int terms of log basics

int digits= (int)(Math.log10(nums[i]))+1;

4)Question specific constraints trick
5)Bit manipulation
THESE BOTH WILL BE COVERED LATER
Question3. Max Consecutive Ones — 485 (Simple counting)

We simply iterate and check if element is zero then the streak ends and we update streak using Math.max

Question4. Squares of a Sorted Array — 977 (Two pointers intro)
Input: nums = [-7,-3,2,3,11]
Output: [4,9,9,49,121]
So the problem here is the negative integers everything is already sorted so the TRICK  here is after squaring we need TWO pointers 
     

[-7,-3,2,3,11]  this becomes [49, 9, 4, 9 , 121]  so between there somewhere there is a point around which everything is already sorted something like [49, 9,(that point) 4, 9 , 121]
 So we use two pointers a start and an end pointer and sort the array by shifting them 

Question5. 🔥Duplicate Zeros — 1089 (Array modification)

Main idea is we go from right to left, we have imaginary pointer which exceeds the array length and we just write when its within range.
Step1) Count no of zeros/extra elements
Step2) we traverse right to left and simply shift each element if j<n & if the lament is zero we shift the j pointer left once and write a zero again 

Things to remember in this code is 
First we use index to shift all elements
Second if the number is zero we shift one left and and write zero

Question6. Merge Sorted Array — 88 (Two array merging)
We will have three pointers one for first reading array, one for reading second , third for writing first, simply compare elements, then place, thing to remember here is the first array can be fully read before the second so then we will need to check if any left in second array and have ablock like 
while(j >= 0) {
           nums1[k] = nums2[j];
           k--;
           j--;
       }

Question7. Remove Element — 27 (In-place removal)
We have one pointer that reads/iterate through array and checks the what elements are not equal to value, and then we have another pointer for writing the elements.
Question8. Remove Duplicates from Sorted Array — 26 (Two pointers)
 Again two pointers, one pointer reads the array , here the condition is as it is ascending order you just need to check if right element is greater than the left element also what we then just switch the element with the other one 

Question9. Check If N and Its Double Exist — 1346 (HashSet usage)
             Brute force simple – two for loops and check for each element if next exists 
             Hashmap – make a hashmap add with key as element and index as value 
		Then simply check through containskey (function) if 2*element exists and the                                                
                        index is not equal to i
             Hashset – make an hashset so here the thing to remember is first we check and if it   
                            doesn't exists then we add, so the flow is like 
First make a hashset
Check if the nums*2 exists and have another || condition (nums%2 =0 && set.contains(nums/2) return true
Else add the nums to set

Question 10. Valid Mountain Array — 941 (Single pass validation) 
 So the way to think here is think about peak, there will a increasing side and an decreasing side, have two pointers and increase one if the elements are increasing and decrease one (from the end) if its decreasing from the other, if they meet at the same its a peak.
Things to consider edge cases - peak is not on one of the ends for example the first element or the last 
Question 11. Replace Elements with Greatest Element on Right Side — 1299 (Right-to-left)
	Brute force- for left to right, check the greatest for each element and then update each        
                                element 
Optimal - Go from right to left and have a variable that keeps track of the max element and the question in my mind was how are we going to tackle the update and still read problem so we simply make a variable to check 
Question 12. Move Zeroes — 283 (Two pointers with zeros)
Simply have two pointers one to iterate through (using the for loop) and have one pointer that just holds the position of the zeros that we need to make a switch here and then we have a temp variable to make the switch 
Question 13. Sort Array By Parity — 905 (Partitioning)
We have two pointers one at start and one at end then we will have 4 cases 
	Odd , even then we swap i++,j–
	Even, odd then we do nothing j- -
	Odd, odd we do nothing  i++
	odd , even we do nothing j - -
Question 14. Sort Array By Parity II — 922 (Even/odd positioning)
           I=0 AND J =1  and we check if i is even do nothing i+=2 similarly for j check if odd and increment by 2 places and then at the end of each for loop simply swap 
So how it looks is 
One while loop
	while(i condition)
	while(j condition)
	if(i & j both in bounds)
		Swap

Question 15. Height Checker — 1051 (Comparison with sorted)
	So we simply make a new array and then we clone and sort array using int sorted[] = heights.clone();Arrays.sort(sorted);
After that we have a simple for loop and check if each element is equal, if not counter increment and return counter 

Question 16. Third Maximum Number — 414 (Set for top elements)
	Basic approach- turn array to set, set to array then sort and give the max 			      
            Use collections-                                         Set<Integer> set = new HashSet<>();
        for(int num: nums) set.add(num);
        if(set.size()<3) return Collections.max(set);
        set.remove(Collections.max(set));
        set.remove(Collections.max(set));
        return Collections.max(set);
	Use TreeSet/ordered set
	Use Three variables 


Question 17. Find All Numbers Disappeared in an Array — 448 (Array as hash)
Brute force - have 2 for loops, we just find if the number is present if present ok , if not and to check use this condn  if(i == nums.length){ missing.add(number);
If not present simply add in arraylist

Inplace mark negative- simple for each element mark that index’s element as negative then when that element is not negative add the element

Question 18. Single Number — 136 (XOR properties) 
	So XOR is like only true when both bits are different , so what we can do here take xor of all the elements, rest of the elements will automatically become 0 and we will only be left with the distinct number.
And for implementation’s purpose we will start from 0 as xor of any number with zero is number itself.
4 → 0100
0 → 0000      
            4 XOR 0 → 0100    
So if we have an array like [4,2,1,2,1]    4^2^1^2^1 → 4^0^0 → 4^0 → 4    


Question 19. Majority Element — 169 (Boyer-Moore or counting)
Brute → for loop ek ke andr ek , for each count kro total if occurrence > n/2 return number.
Hashmap → hashmap me daldo use getorDefault, same n/2 ka khel
Sort and give middle Approach   sorry and give n/2th element
Booyre Moore Voting → have a candidate(current) for each occurrence add to its count and when the count is zero , change the candidate and at the end of the array, whichever candidate is current is the majority or winning candidate.
Bit manipulation → count the the number of 1s in each position for each element , and on basis of that judge what is the numbers bits , also we need to do 32 bits as integer is 32 bits long


Question 20. Contains Duplicate — 217 (HashSet detection)
Simply add to hashset and check if length is equal or not 
Sort and check adjacent 
Add to hashmap if value for any key greater than 1, return duplicate 
Streams, convert to streams, 
	return Arrays.stream(nums)
             .distinct()
             .count() != nums.length;

Question 21. Best Time to Buy and Sell Stock — 121 (Single pass min tracking)
Brute force - check profit for each element towards all the elements to the right, simple nested for loops technique
Another approach is kind of clever , so we have use elements as as iteration , then we find minvalue we encountered till now, then we have a maxprofit , a profit
We check the profit from mintillnow and calculate profit and then compare it with maxprofit
class Solution {
   public int maxProfit(int[] prices) {
     
       int mintillnow =Integer.MAX_VALUE;
       int maxprofit=0;
       for(int price :prices){
           mintillnow =Math.min(mintillnow,price);
           int profit = price- mintillnow;
           maxprofit=Math.max(profit,maxprofit);
       }
       return maxprofit;
   }
}
 

Question  22. Find Pivot Index — 724 (Left vs right sum)
	So what we can do is have the total and calculate right and left sum towards each element and compare the numbers right sum = total - left sum - current element 
If ls == rs return i and if they not return -1;
Question  23. Pascal's Triangle — 118 (Row generation)
So the left and right most elements are 1 and rest ith term are the sum of previous rows (i-1)th + (i)th term 

So the tactical thing is we create a list of integers, and then add this list for each iteration to a list of list of integers that we are gonna return 


Question  24. Pascal's Triangle II — 119 (Space optimized)
Same ;like previous but we need a more optimized solution these all are o(n^2)
So a better approach is using the combinations formula, where i be the row and k be the element then iCk is the kth element 
And the tactical deplyement is like 
nC(k+1) = nCk × (n-k) / (k+1)    Get next value from current
nCk = nC(k-1) × (n-k+1) / k		Get current from previous (used in code)
Question  25. Find All Duplicates in an Array — 442 (Array as hashmap)
So the problem here says that there should be linear space utilization so the problem is when we use hashmaps, hashsets, sorting those approaches cant follow the restrain , so what we can do instead is marking the index or elements as negative , so an algorithm like 
Step1)having a list named result
Step2)calculate index 
Step3)check if nums[index] is negative
	If yes then add to result\
	In not , make the element negative

Question 26. Reverse String — 344 (Two pointers swap)
Simply use two pointers, one at the strating end, other at the other end, and simply use a temp and swap elements
Question  27. Valid Palindrome — 125 (Two pointers validation)
SO tactical points here are just use while(l<r && !Character.isLetterOrDigit(s.charAt(l))) l++
And then just checking if charAt(l) is equal to charAt(r)
Question 28. Remove Duplicates from Sorted Array II — 80 (Count-based removal)
So basically what we trying to have 2 pointers and we have one reading index that is a for loop, then we have a write index and we compare the writeindex-2 simply if the readindex is not equal to writeindex-2 we update the write and simply ++the write index 
Question 29. Two Sum II - Input Array Is Sorted — 167 (Two pointers sum)
As the array is in ascending order what we can do is have two pointers one at start and the other at end and simply return the index if sum is equal to target and if this is not the case then what we can do is compare the sum and target and accordingly  move left, right index 
Question 30. Reverse Vowels of a String — 345 (Conditional swap) (nice question)
SO first thing first strings are immutable so convert to array use char[] arr =s.toCharArray();
Next tactical thing is we need to check if a character is vowel we simply create a string named , vowel and then use vowels.indexOf(arr[i])==-1) 
And then we simply swap and i++,j--   

Question 31. Intersection of Two Arrays — 349 (Set intersection)
Have two sets first set add the nums1 and then in second set only add elements that are present both in nums1 and nums2 and at the end we convert the set to an array using streams return result.stream().mapToInt(i -> i).toArray

Question 32. Container With Most Water — 11 (Two pointers greedy)
Have two pointers, left and right and simply calculate width=right-left ; height=Math.min(height[l],height[r])
For two pointers which ever of left and right pointer’s height is smaller move that 

Question 33. 3Sum — 15 (Sorted two pointers)
First we sort the array, we sort it so we have a sense of directional order that way we can manipulate it.
THEN we have three pointers i(an outer loop)
J as i+1 for each iteration, then k as length-1  
We check if nums[j] +nums[k] = target(ie. -nums[i])
Then we check if sum(ie. nums[j]+sums[k])   if sum<target then j++ because array is sorted   Else k--  
Also other important thing is we dont want duplicates in the result so we put a conditionFor i at the start of for loop  if(i>0 && nums[i-1]==nums[i]) continue;
Then two while loops for j & k if(j<k && nums[j-1]=nums[j] j++;
if(j<k && nums[k]=nums[k+1] k--;
Question 34. 3Sum Closest — 16 (Distance optimization)
Same like previous question have 3 pointers same technique only this time the difference is we need to find closest so we take abs difference and then play accordingly , have a current sum and a closest sum comparing them updating them 








Question35. Sort Colors — 75 (Dutch flag partition)

Main idea is we have three pointers as boundaries all 0’s right to low 
To whenever nums[mid]=0 here swap low and mid element mid, low++
If mis=1 mid++
If mid==2 swap aur sirf high--   matlab us element ko mid ke right me daldiya 
So three pointers but think the range of those we have some thing like 


low=0;mid=0;high=n-1;
Elements sirf 0,1,2 hi honge , to apn mid se khele khel 
if(nums[mid]==0)  should be then swap nums[mid] and nums[low] because 0 should be right of low and we mid++ and low++
if(nums[mid]==1) to simply mid++
else(nums[mid] ==2) swap nums[mid] and nums[high] and mid++ and high -- 
 

Question36. Valid Triangle Number — 611 (Triangle inequality)
So basically we sort the array, the idea behind sorting is to be able to play with the longest side so we simply loo[pp around the longest side 
If sum of smaller sides is longer than the longest side then it is a triangle , so the thing is array is sorted so all the combinations from i to j which are (j-i) ways of triangles 
To count =+j-i; j–;
Else i++;

Question 37. Boats to Save People — 881 (Greedy pairing)
Apnne sort krlena hai, fir simple best case if heaviest goes with lightest if possible if not heaviest goes by himself.
if(heavy+light)<=limit;{
Light++;}
heavy--  ; heavy always going in a boat;
Boats++; 





Question 38. Bag of Tokens — 948 (Two pointers strategy)

Jha bhi greedy abhi tak , bas sort krdo array tbhi you can use it greedily. 
Most greedy approach hogi sort krke cheapest token se score khredna (FACEUP)that means l, (FACEDOWN) me greedy hoga score tab spent jab token[h] max . 

	
Sliding Window Introduction (39–52)
Question 39. Maximum Average Subarray I — 643 (Fixed window sum)
Dont always think in two pointer, for a window if its length is fixed we not need to have two pointers for each of its boundaries, instead use the length of the window.
Calculate sum of first window, then just add next element to sum and then remove previous 
Question  40. Minimum Difference Between Highest and Lowest of K Scores — 1984 (Sorted sliding window)
First sort the array, that way we already know the highest and lowest for k numbers, then we simply subtract and see what is min diff
Question  41. Grumpy Bookstore Owner — 1052 (State-based window)
First find all the satisfied customers , now the problem is if we could flip as many not satisfied to satisfied(ie is non grumpy), we basically need to maximize the number of unsatisfied customers.
Then we simply add satisfied and the maxunsatisifed customers in that k minutes of time, using the window 
Question 42. Max Consecutive Ones III — 1004 (Window with K flips)(thoda yaad rkne type ka hai)
Variable pointer windows apan a lot of times solve by 2 pointers with sliding window 
Have two pointer land r , r iterates the array and first we expand the array until max zeros possible , after that we start to shift left pointer 
Question 43. Longest Substring Without Repeating Characters — 3 (Variable window)
	Actual trick is using hashmap, 
Two pointer chalane hai teji se, sliding window bhi hai but sirf contextual/pragmatism of problem, otherwise computation me khi bhi ye involved nhi hai         l,r=0;
To trick is jab aisa element mile jo already in hashmap hai,l ko update in such a way that last appearance +1; index  so l =map.get(c) +1;, fir maxlength math.max se update krte rhte .length of current window=l-r+1

Question 44. Minimum Size Subarray Sum — 209 (Shrinkable window)
Simply ek left aur right pointer ho , right se sum lete jao , aur jab sum bada hojaw target se tab , then ek while loop jo l++ karta jae and check krta jae minlen
Question 45. Longest Repeating Character Replacement — 424 (Character frequency)
So the trick here is that we can switch atmost k times, and any valid substring would be lengthofwindow - mostfrequentelement <k , if not we simply move the left pointer towards right
Question 46. Fruit Into Baskets — 904 (Two distinct elements)tricky implementation 
Same concept of window, things to consider is how you move window and what condition you apply
So window has l and r , we move r to read array at every iteration, move l++ when map.size>k , and update value of map.get(leftfruit)-1;remove if map.get(lf) ==0
And update maxtree len, by math

Question 47. Permutation in String — 567
Have two arrays for freq counting one for s1 and one for a window of len =s1(we try to maintain it to s1)
Populate s1count, populate windowcount with s2 such that if element or i of s2 is >=s1.len
Freq of leftmost -- in windowcount array, then if both arrays are equal return tru otherwise false
Question 48. Find All Anagrams in a String — 438 (All anagram positions)
Whenever we want to find if a string’s some permutation is present or not we can simply think of frequency arrays for each of the string , this is same like previous questions ,
We again simply decrease the frequency of the leftmost element when window is getting longer than p.length() and at the end simply use if Arrays.equal(array1, array2)
Question 49. Longest Substring with At Most Two Distinct Characters — 159 (K=2 distinct)
So the questions is like the final substring must have 2 distinct characters, simply think of a hashmap and use the technique every time a new element appears to right we reduce the count in hashmap as soon as the count of the left element zero remove it , but we always will shift left point to right
Main point is understanding that hashmap length represents the number of distinct elements
Question 50. Longest Substring with At Most K Distinct Characters — 340 (Generalized K distinct)
Same like last problem main thing, is that theres a hashmap whose length is distinct ele,
We havea left and a right pointer , we move right and put it in hashmap, fir agr more than or k elements hojae jab hasmap to left wla ++ krke uska count decrease 

Question 51. Subarrays with K Different Integers — 992 (Exact K distinct)
This problem is also similar to previous ones, we simply calculate maximum number of substrings/subarrays present, for example we calculate atmost 3 it include with 1, 2 elem
So we simple subtract with atmost(k)-atmost(2) == with(k)
Question 52. Longest Subarray of 1's After Deleting One Element — 1493 (One deletion allowed)
So we are trying to find the longest window with utmost one zero.
We go by solving it such that if number of zeros is greater than 1 , we shift left pointer 




HashMap Fundamentals (53–68)
Question 53. Two Sum — 1 (Classic hashmap)
Simple use hashmap, have a compliment , so what are we trying to add in hashmap, element and its index, so for current element if we have its compliment then return the that i and the compliments(the other elements) index 
Question 54. Valid Anagram — 242 (Frequency counting)
Two frequency arrays , like a to z each element representing an alphabet

Question 55. Contains Duplicate III — 220 (Value and index constraint)
Either use a bucket sort , we used treeset (o of nlogk), so the idea is to use floor and ceiling, we simply calculate the floor.
We have two if conditions one for floor where we check floor is not null and if num- floor <= valuediff and similarly for the ceiling.
And the sliding window part sixmply , agr i>indexdiff to remove (i-indexdiff) or the element outside the scope.USE LONG
Question 56. Intersection of Two Arrays II — 350 (Frequency map)
To apn frequency array use krenge ,create them , populate them and then compare fir utti baar print krdo 
Print krne ka logic, 
Ek for loop ke andr ek aur , but ye n^2 complexity nhi hai , o of n for some reason most probably because of input or some logic 
Question 57. First Unique Character in a String - 387 (Frequency + order)
Simply use a freq array or a hashmap,
Step 1)populate the array/hashmap
Step 2)simply ek for loop, jiska pehla 1 agya , wo answer i
Question 58. Ransom Note — 383 (Character availability)
Simply use frequency arrays and then if frequency of each element/alphabet for ransomnote is less than magazine return false, else return true;
Question 59. Isomorphic Strings — 205 (Character mapping)
So what we do is we remember the first occurance of characters for the strings, if they are not equal return false 
Question 60. Word Pattern — 290 (Bijection mapping)
Whenever dealing with hashmap remember we mostly check if something is not present if we try and see something is present what happens is the first time we encounter it we return a true and its broken because we dont parse rest of the array 

So here we plan to have to Hashmap one chartoword and the other wordtochar, we then check if chartoword maps to that word and wordtochar maps to that character. 
Question 61. Group Anagrams — 49 (Signature grouping)
TO scene aisa hai, ek hashmap banae of <String,list<String>> ka, fir each element ko sort krke ,usko arralist me add krte jae, also java me string immutable hoe, to apn kya krte convert it to an array of char[] chars = stringname.toCharArray()); fir sort krke convert 
Question 62. Happy Number — 202 (Cycle detection)
So the thing is when we do the happy number check again and again, a loop is formed ,
We can check this loop by two methods 
USE a set, if its a set the loop doesnt infinite as the number of duplicates are eliminated 
USE two pointer, one fast and one slow=n, fast=nextnum(slow/n);
Fir if fast pointer is equal to slow , false but code krna hai opposite code kaise likhoge
while (fast != 1 && slow != fast) {
           slow = getNext(slow);
           fast = getNext(getNext(fast));
       }
      
       return fast == 1;



Question 63. Contains Duplicate II — 219 (Index tracking)
Have a hashmap with the element as key and index as value, if map contains the element and i-that index<= k , then return true , else add the element to the map
Question 64. Longest Harmonious Subsequence — 594 (Adjacent frequencies)
Have a hashmap , populate it with the elements and their frequencies , then use map.Keyset() and in a for loop check if hashmap has ele+1;  then add them and give maxlen using Math
Question 65. Degree of an Array — 697 (Frequency + range)
So here the approach is for every element we need to keep in account, three things , count/frequency , first appearance, last appearance. We use three hashmaps to keep track of them.After that for all elements whose count == degree , we calculate window r-l+1;
We use int degree=Collections.max(count.values()); to find degree 
         For syntactical finesse   for(Integer num : count.keySet()){

Question 66. Set Mismatch — 645 (Duplicate + missing)
We simply have a hashmap record everything, then have a for loop if something missing add it as missing, if somevalue is 2 add it as duplicate and return ,

You can also use a array as the length , nums[i] is ok not too big , something similar to a frequency array
Question 67. Valid Sudoku — 36 (Multiple sets)
Matrix dekhke bharbharae nhi, simply ek set kre of strings, aur har ek element ko unique 
Do for loop ho ,kaise banae , ek element ko teen tareeke se store kre row→ c + “in row” + i
Col→ c + “in col” + j    box→ c + “in box” + i/3 + “-” + j/3
Ok ab agr new element me kuch bhi contains hoga to false either add
Question 68. Logger Rate Limiter — 359 (Time-based hashmap)




Prefix Sum Pattern (69–78)
Question 69. Range Sum Query - Immutable — 303 (Prefix sum query)
Constructor, method, class, ye sab samjh nhi arhe hai sahi se, logic is ok
Ki prefix sum calculate krlo ki prefix[0]=nums[0], fir prefix[i]=prefix[i-1]+nums[i];
Fir return ke time if left==0 return prefix[right]
Else prefix[right]-prefix[left-1] because l and r inclusive chiaye
Question 70. Range Sum Query 2D - Immutable — 304 (2D prefix sum)

class NumMatrix {
  
   private int[][] prefix;
   public NumMatrix(int[][] matrix) {
       int m=matrix.length;
       int n=matrix[0].length;
       prefix = new int[m][n];
       for(int i=0;i<m;i++){
           prefix[i][0]=matrix[i][0];
           for(int j=1;j<n;j++){
               prefix[i][j]=prefix[i][j-1]+matrix[i][j];
           }
       }
  
   }
  
   public int sumRegion(int row1, int col1, int row2, int col2) {
       int sum=0;
       for(int i=row1;i<=row2;i++){
           int leftprefix=0;
           if(col1>0){
               leftprefix=prefix[i][col1-1];
           }
           else{
               leftprefix=0;
           }
           sum+=prefix[i][col2]-leftprefix;


       }
       return sum;
   }
}


Question 71. Matrix Block Sum — 1314 (2D range query)
Same whoi matrix ka presum bnaya fir, answer/sum calculate krne ke liye , logic lgado , logic copy pe jaega , fir whi row wla for loop lgana
Question 72. Product of Array Except Self — 238 (Prefix/suffix products)
Total product lele, fir 3 cases hai main, agr †wo  0 then then all elements are 0, if 1 elelement only element with 0 gets product, if no eros simple pro/nums[i]
Question 73. Subarray Sum Equals K — 560 (Prefix sum + hashmap)
So we use prefix and the condition is prefix[r]-prefix[l-1]=k, when ith element is inclusive 
We can say prefix[r] -k =prefix[l-1]  , we use a hashmap<prefix, frequency> and say if we find the prefix[r]-k then it is a valid subarray.also we move from left to right so we have all previous possible combinations
Question 74. Continuous Subarray Sum — 523 (Modulo prefix sum)
Think similarly we are trying to find (prefix[r]-prefix[l])%k==0, this can also mean 
prefix[r]%k==prefix[l-1]%k
Bas fir who hashmap, if found then valid subarray does exists 

Question 75. Subarray Sums Divisible by K — 974 (Modulo handling)
Same to previous question same technique only thing unique here is we need to somehow accommodate the negative remainders so we use 
int curr=((prefix[i]%k)+k)%k;
And we play usong frequency 
Question 76. Contiguous Array — 525 (Transform to sum problem)
So its a binary array and we need to find equal number of 1 and 0 , so sum =0;
We convert 0s to -1s , prefix me prefix[i]=prefix[i-1]+((nums[i]==0) ? -1:1);
And simply agr koi i ke liye current=prefix[i] apn dekh chuke to uss wo ek valid window hogyi, after that what we do is windowlength= i-map.get(current) fir just measure maxlen

Question 77. Maximum Size Subarray Sum Equals k — 325 (Longest subarray)
Same hi hai 

Question 78. Subarray Product Less Than K — 713 (Product constraint)
Sliding window use krenge, l,r=0;
Ek for loop se product calculate aur fir use for loop ke andr ek while(pro>=k) to l++;
Main part is count=r-l+1;
Ek subarray [1,2,3,4] whose indexes looks like [5,6,7,8] r=8;l=5 count=8-5+1=4 windows which are → [5,6,7,8] ; [6,7,8] ; [7,8] ; [8]   so 4 possible windows ending with the last element.




String Patterns (79–94)
Question 79. Reverse Words in a String III — 557 (Word reversal)
So we are reversing the words, but keeping the order same of the words same,main thing here ise remembering syntax
Strings are immutable, L*** kuch nhi kr skte direct string se, to new stringbuilder bnao, 
Reverse stringbuilder me krte, append isme krte, aur kya yaad rkhne ka 
Array of string bnate using split  String[] words= s.split(" ");
Fir stringbuilder se wapis string bnate String reversed=new StringBuilder(word).reverse().toString();

Question 80. Longest Common Prefix — 14 (Vertical scanning)
To apn first word uthaenge,2 for loop lagenge but compelxity n2 nhi hogi beacuse comparisons utte nhi ho rhe, to logic hai agr characters same nhi to substring return kro strs[0] ki (0,i) tak 
Yaad kya rkhna hai ki substring kaise use krte
Question 81. Valid Palindrome II — 680 (One deletion allowed)
So we use a ispalindrome function, so here the question says utmost 1 deletion, so the logic if there is a mismatch of l and r then we can delete one that means we check ispali(s,l+1,r) or ispali(s,l,r-1)  
Question 82. Longest Palindrome — 409 (Character frequency)
To logic here is agr koi element 2 baar hai to valid hai, or multiple of 2 to valid, agr odd hai , to frequency-1, aur end me agr koi single present hai to len+1;  two cases where you get singles , when some element appears only once , then when the frequency is odd, you can use a boolean variable, also to accommodate uppercase in the frequency array 
if(c>='a' && c<='z'){
               arr[c-'a']++;
           }
           else if(c>='A' && c<='Z'){
               arr[c-'A'+26]++;
           }

Question 83. Repeated Substring Pattern — 459 (Pattern detection)
So have a for loop where we check each string, and number times we can repeat it is s.len/len of that string,
Also we need to only check upto n/2, and also we usse substring to extract the string 
Question 84. Find the Index of the First Occurrence in a String — 28 (Substring search)
To apn , use krenge substring, aur ek loop laganege 0 to hlen-nlen tak ka, aur use krnege 
if(haystack.substring(i,i+nlen).equals(needle))

Question 85. Longest Palindromic Substring — 5 (Expand around centers)
So basically we create a expandarouncentre function where we just check if left and right elements are equal and then for the main body we have a for loop and then each i as a centre we expand both ofor odd and even , and then return the longest while comparing it to the result.
Question 86. Palindromic Substrings — 647 (Count palindromes)
Simply use the very same expand the centre logic, and this time just use a counter, and for the main body simply use a for loop and then count++ around for odd and even strings.
Question 87. Find and Replace Pattern — 890 (Pattern matching)
Bijection, two way mapping, their complexity because we have a list of strings , so we can create a function that provides if its a valid match or not.
And in that we have two hashmaps wtp and ptw , and we check if the first char of each of them map to other, we do this for both maps, we check if w is present and if present equals to p or not.
Question 88. Group Shifted Strings — 249 (Normalization)
Locked hai, nhi kr rhe 
Question 89. Decode String — 394 (Stack-based parsing)
Have two stacks stringstack and count stack 
4 possibilities → [  or number or ] or alphabet
[ → push in sstack
] → sstack ka pop krke ek stringbuilder me dalte, agar empty nhi to pop krte aur fir new string= ko repeat krte cstak.pop times 
Baki syntax kafi tha Character.getNumericValu    Character.isDigit
!sstack.peek().equals("[")

Question 90. String Compression — 443 (Run-length encoding)
class Solution {
   public int compress(char[] chars) {
       int read=0;
       int write=0;
       while(read<chars.length){
           char current=chars[read];
           int count=0;
           while(read<chars.length && chars[read]==current){
               read++;
               count++;
           }
           chars[write]=current;
           write++;


           if(count>1){
               String str= String.valueOf(count);
               for(char c:str.toCharArray()){
                   chars[write]=c;
                   write++;
               }
           }
       }
       return write;
   }
}

Question 91. Compare Version Numbers — 165 (String parsing)
Tricky problem statement, multiple decimals /versions ho skte, fir unme teen case hote , ya to 1,-1,0. String ko split krlo into an String[ ]  , jo phle bada mila uss hissab sse set krdiya 
Question 92. Multiply Strings — 43 (String arithmetic)
Answer store karwana padega using two pointer , ek result array bnalo of (m+n) digits, 
Right to left it represents digits rightmost being untis place, extract digits rgiht most , mulptiply them, two pointers pos1=i+j (carry wali)  pos2= i+j+1(last digit), sum bhi calculate krna = product+last carry that is result[pos2] 
Fir result[pos1]+=sum/10; result[pos2]=sum%10;
Question 93. Add Binary — 67 (Binary addition)
While loop use , 0,0 =0; 0,1=1;  1,1=1, carry 1
Ek result name ka  integre array, fir uski length maxlen+1;
If else conditions with ternarny 
Fir leading zeros ka dekhna hai 
 Question 94. Repeated DNA Sequences — 187 (Rolling hash)
Simply have a hashmap, add substrings , for i, have aresult list, add if already seen, if not add to hashmap


More Arrays + Greedy Introduction (95-109)
Question 95. Rotate Array — 189 (Cyclic rotation)
Ek function bnao jo reverse kre, fir last k elements reverse, then 0 so k-1 tak elements reverse, fir whole array reverse
Question 96. Next Permutation — 31 (In-place permutation)    ⭐️
So the algorithm is like
From right to left , we try to find a pivot, which has a bigger number towards right.
Once we find that, we find the smallest bigger number towards its right ,swap them 
And then reverse the pivot+1 till end 
Question 97. Merge Intervals — 56 (Interval merging) ⭐️
Step1)Sort the list Arrays.sort(arrayname, (a,b) ->(lambda) a[0]-a[b];
Main thing is remembering that how are we going to manage the merging logic, to ek int[] current \ength of 2 ka lelenge,if window overlap update current , current ka[1] hi  update sirf  if overlapping, fir baas add to result 
Question 98. Insert Interval — 57 (Interval insertion)
So what we need to do is, first add the intervals which are not merging, to the ones while (i < n && intervals[i][1] < newInterval[0])
Then we need to merge the ones which overlap , we dont use a overlapping conditions, instead we get there automatically because last while lopp leaves us at the overlapping intervals,while (i < n && intervals[i][0] <= newInterval[1]) {
           newInterval[0] = Math.min(newInterval[0], intervals[i][0]);
           newInterval[1] = Math.max(newInterval[1], intervals[i][1]);
           i++;
       }
Then we simply add the rest while (i < n) {
           result.add(intervals[i]);
           i++;

Question 99. Non-overlapping Intervals — 435 (Interval removal)
Keep track of previous non overlapping,s ending, and then compare it to the current ith element’s start, if its smaller its a overlapping one, simply increment the counter.
Question 100. Assign Cookies — 455 (Greedy assignment)
So most of the time to use greedy anything, you need to know what is more valuable , so sorting is the way to go.have two pointers then the same thing compare  g[i] & s[j]
And move accordingly 

Question 101. Lemonade Change — 860 (Change making)
Bass possibilities calvculate krlo , uske baad sirf if lese me lga do , bas aise ki five always save krna hai priority is to save fives always.
Question 102. Meeting Rooms — 252 (Overlap check)
Locked hai
Question 103. Jump Game — 55 (Greedy reachability)
Simply har ith element pe calculate krna hai maximum position possible, jb bhi koi len-1 ke equal y zyada hojae to sahi bdado 
Question 104. Jump Game II — 45 (Minimum jumps)
Instead of trying to find out if we can get to end, we need to find the minimum number of steps it takes to get to end. So we do is have windows, have a currenend that represents the maximum for the current number and a max that is the maximum tht we have recorded till now , then main thing here is if if(i == currentEnd){
               count += 1;
               currentEnd = max;
           }


Question 105. Gas Station — 134 (Greedy circuit)
So greedy approach here is if we cant reach/satisfy condition from ith position so you cant satisfy from any positions left to i. So whenever a condition fails we take greedy decision 
if (tank < 0) {
               start = i + 1; // reset window
               tank = 0;
           }

Question 106. Partition Labels — 763 (String partitioning)
Two ways hago , one is o(n2) and other single pass,
Have a array named last that records the last occurrences through 
int[] lastIndex = new int[26]; for (int i = 0; i < s.length(); i++) { lastIndex[s.charAt(i) - 'a'] = i; }
After that we start a for loop ;
And end = maximum of(end, the last occurrence of th etih term )
Uske baad if i==end add result to partition
Aur start update krdo 
Question 107. Minimum Number of Arrows to Burst Balloons — 452 (Interval coverage)
Arrays.sort(points, (m,n) -> m[0]-n[0]);
       int count=1;
       int end=points[0][1];
       for(int i =1;i<points.length;i++){
           if(points[i][0]>end){
               count++;
               end=points[i][1];
           }
           else{
               end=Math.min(end,points[i][1]);
           }
       }
       return count;

Question 108. Candy — 135 (Two-pass greedy)
Question 109. Queue Reconstruction by Height — 406 (Greedy reconstruction)

## LinkedList Fundamentals (110-134)
Question 110. Reverse Linked List — 206 (Iterative reversal)
Have three pointers prev, curr ,    next  but also remember we just want to temporarily save the temp just so that we can work right now 
Loop lgado, fir curr.next=prev; prev= curr; curr=next 
Question 111. Middle of the Linked List — 876 (Fast/slow pointers)
Naive approach, find the middle using count, then again loop and return the list node which is at middle.
FAST/SLOW POINTER APPROACH
Fast moves two steps and slow moves , and we check if fast!=null and fast.next!=null this was for while loop
Question 112. Palindrome Linked List — 234 (Middle + reverse)
Find middle, slow and fast pointers , after that reverse after slow/mid, and then compare first and slow and so on
Question 113. Merge Two Sorted Lists — 21 (Merge operation)
Make a dummynode , then we compare list1 and list2 nodes, then add to the next of dummynode, and increment/next the list, and then simply fill the non empty list when one list is empty
Question 114. Remove Linked List Elements — 203 (Value removal)
ListNode dummy=new ListNode(0);
       dummy.next=head;
       ListNode curr=dummy;
       while(curr.next!=null){
           if(curr.next.val==val){
               curr.next=curr.next.next;
           }
           else{
               curr=curr.next;
           }
       }
       return dummy.next;

Question 115. Delete Node in a Linked List — 237 (Node deletion)
SO the pro=blem here is we are not given the head, so instead we add the value of next in current and skip the next 
node.val= node.next.val;
       node.next=node.next.next;

Question 116. Remove Duplicates from Sorted List — 83 (Adjacent duplicates)
Dummy lgado, if curr.value == curr.next.value, curr.next ko curr.next.next;
Return dummy.next 
Question 117. Linked List Cycle — 141 (Floyd's detection)
Kuch nhhi bas, ek slow , ek fast lena hau , fir slow ek , fastnext.next , fir if equal cycle exists .
AUR QUESTION STATEMENT ME JO pos  DIY HAI, SIRF TO CONFUSE
NHI  BHAI AGLE QUESTION PE SMJH AYA - its important that you write the sand f iteratively,warna apko kaise pts hoga tail ke baad kha jna hai, while trying to predict the next iterative step.
Question 118. Linked List Cycle II — 142 (Cycle start)
Phle ek cycle to find if cycle exists, if exists , we reset the slow pointer to head and then return slow pointer Phle ek cycle to detect cycle, if cycle exists, we break and reset slow=head , while keeping fast as same then another cycle, slow is the starting point of the cycle,
Question 119. Intersection of Two Linked Lists — 160 (Length difference)
So what we wo do is find the length , which ever is longer , uska pointer length- lengthb se age badado
Fir balance hogya, to find the intersection simple loops se 
Question 120. Remove Nth Node From End of List — 19 (Two pointers)
Intuition simple basic iteration find len, len-n is the point,from which we skip the next element.
But slow and fast idk how to know fast is kept at n fir fast chalate end tak.

Question 121. Rotate List — 61 (Circular connection)
Turn it into a new circular list point the current tail to head, then len-k-1 th element ko new tail bnao aur uske agle ko newhead jo already sahi jagah point kar rha hai 
Question 122. Odd Even Linked List — 328 (Position grouping)
odd.next=even.next;//se
while(even!=null && even.next!=null){   
//even always reaches null first or inworst case equal to odd,ye mere hisab se hit adn trial se aya 
           odd.next=even.next;//sets address of next
           odd=odd.next;//listnode representing that specifc node
           //similarly for even
           even.next=odd.next;
           even=even.next;
       }

Question 123. Partition List — 86 (Two-list approach)
Simply make dummy nodes for smallhead, bighead, and simply assign small and big to the heads, and curr=head,simple loop
			If value kam 
small.next=curr;move small
Else big.next=curr,move big
In the end small ki tail =bighead;
Big ki tail null
Question 124. Swap Nodes in Pairs — 24 (Pair swapping)
Have multiple pointers, one for previous, one for first, one next, one curr, one second
Baki logic bnaloge , copy pe likho bas 
Question 125. Reverse Linked List II — 92 (Sublist reversal)
3 steps
1)find before and after nodes
2)a for loop that reverses from l to right
3)connect the before to right(curr prev) and lefthead/l node to after
Question 126. Reorder List — 143 (Middle + reverse + merge)
Again 3 steps) 
step1)find middle by fast and slow
step2) reverse the second half
Step3)join alternatively 
Question 127. Remove Duplicates from Sorted List II — 82 (All duplicates)
Have a curr, prev, dummy
Ek if condition hogi jiske andr ek while loop thru which we check the repetitive ones and then just skip them using the while loop and update prev to curr.next ultimately skipping the current recurring number 



Question 128.  Elementary addition)
Remember new list bannai hai , to ek dummy se nwe ll bnai jae, curr, curr1,curr2 ho carry ho
Aur new Node jodna hai har baar curr.next = new ListNode(sum % 10);
Question 129. Add Two Numbers II — 445 (Reverse order)
Reverse both lists, then add them normally, then reverse the answer;
Question 130. Plus One Linked List — 369 (Increment operation)
locked
Question 131. Sort List — 148 (Merge sort)
 Merge Sort, recursive calls, use the name of the function that we writing for recursion
So we need a helper function called merge.then we create  dummy and then just compare the l1 and l2 nodes and curr(dummy) to them 
So steps are like 
step1)find middle by slow and fast, then the node before middle disconnect the lists 
while(fast!=null && fast.next!=null){
           prev=slow;
           slow=slow.next;
           fast=fast.next.next;

step2)recursion 
ListNode left=sortList(head);
       ListNode right=sortList(slow);
      
       return merge(left,right);

step3)write merge fn
private ListNode merge(ListNode l1, ListNode l2){
       //remmeber the concept of reaaranging you just take the head dummy and connect & connect
       ListNode dummy=new ListNode(0);
       ListNode curr=dummy;
       while(l1!=null && l2!=null){
       if(l1.val<l2.val){
           curr.next=l1;
           l1=l1.next;
       }
       else{
           curr.next=l2;
           l2=l2.next;
       }
       curr=curr.next;
       }
       if(l1!=null){
           curr.next=l1;
       }
       else{
           curr.next=l2;
       }
       return dummy.next;
   }

Question 132. Insertion Sort List — 147 (Insertion sort)
Have a outer loop , and get a variable named next i.e. curr.next for implementation purpose, have a prev, have a pos, pos is the number that is greater than the current and then we havea another loop where we find the pos, and we just increment the prev and the pos there and when we come outside the second loop we simply insert the current between the pev and pos so prev.next=curr; curr.next=pos and then we update curr to next again 

Question 133. Copy List with Random Pointer — 138 (Deep copy)
So what is happening here is we make a hashmap of nodes, aur hashmap me curr aur ek new node add krte map.put(curr,new Node(curr.val));to ye directly new creation krte 
Next next loop me simply while curr!=null me if(curr.random!=null){
               newcurr.next.random=map.get(curr.random);
           }
Aur move pointers

Question 134. Flatten a Multilevel Doubly Linked List — 430 (DFS flattening)


## Stack Fundamentals (135-152)
Question 135. Valid Parentheses — 20 (Stack matching)
Have a hashmap, for parentheses, then if opening bracket push it, if closing aur uska hashmap me ka hi hai means its th eidentical closing bracket for c , you pop it and at end if stackisempty , answer is true

Question 136. Min Stack — 155 (Auxiliary stack)
To language aur syntax dekhke bharbharana nhi hai , simply logic socha , 2 stack , bahr wale me sirf intialize, fir thode andr me naye bnado ye.fir simply jo bhi apna logic hai , operations ka wo bhar do 

Question 137. Implement Queue using Stacks — 232 (Two stacks)
Have two stacks, one is input and other is output, after that for pop and peek, if the output is empty , we transfer all elements one by one to output until input stack is empty, then we return output.pop, is empty ke liye both stacks empty together 

Question 138. Backspace String Compare — 844 (Stack simulation)
So have a two stacks if char c =# pop else push and then stringbuilder and convert to string and reverse them sb.reverse().toString();
return build(s).equals(build(t));

Question 139. Remove All Adjacent Duplicates In String — 1047 (Stack removal)
Simply if current == stack.peek remove 



Question 140. Remove All Adjacent Duplicates in String II — 1209 (Count-based removal)
Use a stack of pairs 
class pair{
   char ch;
   int count;
   pair(char ch, int count){
       this.ch=ch;
       this.count=count;
   }

Question 141. Baseball Game — 682 (Stack operations)
Have a stack that takes int, and then simply follow the possibilities and at end add all the nums , only thing to remember here is ability to convert a string to int Integer.parseInt(c))

Question 142. Evaluate Reverse Polish Notation — 150 (Postfix evaluation)
Simple hi hai copy/paper pe likho hojai
Question 143. Basic Calculator II — 227 (Operator precedence)
SO what you do to tackle operator precedence is that remember * and / can always multiplied when between two numbers and if its a plus or a minus we add the current number to the stack, then we write it like 
Question 144. Decode String — 394 (Nested decoding)
Syntax thoda complex ho sktat hai ,but main tricks are stuff like remeebering we need to insert at front and to do that we use result.insert(0,sstack.pop());
Aur ek stack count ke liye , ek stack string aur brackets ke liye 

Question 145. Next Greater Element I — 496 (Monotonic stack)
Ek stack ho, aur apan iterate kre nums2 right to left, fir agar peek, smaller hai nums se then we pop it, and add the nums in stack.pop smaller elements as they are useless
Question 146. Next Greater Element II — 503 (Circular array)
So to duplicate a circular array we use % , we simply use a for loop with 2*n-1 and use curr=nums[i%n], then similar to the last one we see if the top is smaller we pop, and then when the second half comes of th iteration i.e. when if(i<n){
               result[i]=stack.isEmpty() ? -1 : stack.peek();

Question 147. Daily Temperatures — 739 (Temperature tracking)
So we can follow the same technique but a counter wont work instead what we do is instead of storing the value in the montonic stack we save the indices of the previous warmer/greater element and then simply the result is stack.peek() - i;

Question 148. Online Stock Span — 901 (Running span)
SO how we tackle this problem is we have an int[] array as stack elements, and we calculate the span for each input, and we simply use the same technique if the next element is bigger we simply pop the previous and addit to span span+=stack.pop()[1];

Question 149. Sum of Subarray Minimums — 907 (Contribution method)  very star question took half a day still im like idk (means the mastery over previous is shallow)no worries pura khatam krke 4 baar padhi din me So left reach nikale , right reach nikale and nums * left * rightreach ++=== ans

 In financial data: if arr is stock prices, NGE = next day the price will exceed today. In temperature data: NGE = next warmer day. In histograms: PSE and NSE give you the left/right wall boundaries for rectangles. The pop event is literally the moment a dominance relationship ends.

PUSH  → "I don't have an answer yet. I'm entering the waiting room."
POP   → "I just got my answer. I'm leaving."
PEEK  → "You're asking about me without evicting me. I'm the answer for someone else."

"What is the NEXT ___ to my RIGHT?"  → answered at POP time
"What is the LAST ___ to my LEFT?"   → answered at PEEK time (before pushing)


Question 150. Largest Rectangle in Histogram — 84 (Area calculation)
Calculate the next smaller, previous mslaeer those are the ranges of the rectangle , and width =nse-pse-1
And the array nse’s -1 should be nums.length and pse’s -1 should be -1
Main idea is what is the biggest rectabglw where this specific bar is smallest height
Question 151. Maximal Rectangle — 85 (2D histogram)
Plan to skip 
Question 152. Car Fleet — 853 (Position + speed)
So what we do is a create a 2d with position and speed as elements of array, then sort it in descending order with position as basis, then we calculate timeof arrival 
double time =(double)(target-car[0])/car[1];
. so the idea is if the time of arrival is more than the previous/stack.peek()   , it would never catchup with current fleet and hence create a new fleet 
And at the end size of stack is is the number of fleets



## Queue & Heap Basics (153-167)

Question 153. Design Circular Queue — 622 (Circular design)
class MyCircularQueue {
   int[] data;
   int head;
   int tail;
   int capacity;
   int size;
   public MyCircularQueue(int k) {
       data=new int[k];
       head=0;
       tail=-1;
       capacity=k;
       size=0;
   }
  
   public boolean enQueue(int value) {
       if(isFull()) return false;
       tail=(tail+1)%capacity;
       data[tail]=value;
       size++;
       return true;
   }
  
   public boolean deQueue() {
       if(isEmpty()) return false;
       head=(head+1)%capacity;
       size--;
       return true;
   }
  
   public int Front() {
       return isEmpty() ? -1:data[head];
   }
  
   public int Rear() {
       return isEmpty() ? -1 :data[tail];
   }
  
   public boolean isEmpty() {
       return size==0;
   }
  
   public boolean isFull() {
       return size==capacity;
   }
}


/**
* Your MyCircularQueue object will be instantiated and called as such:
* MyCircularQueue obj = new MyCircularQueue(k);
* boolean param_1 = obj.enQueue(value);
* boolean param_2 = obj.deQueue();
* int param_3 = obj.Front();
* int param_4 = obj.Rear();
* boolean param_5 = obj.isEmpty();
* boolean param_6 = obj.isFull();
*/

Question 154. Design Circular Deque — 641 (Deque design)
Same like previous but we just need to accumulate the the tail from n-1
Question 155. Moving Average from Data Stream — 346 (Window average)
locked
Question 156. Number of Recent Calls — 933 (Time window)
Simply a queue, first innsert/offer it ,then if queue.peek()<t-3000; remove it/poll();return queue.size();
Question 157. Dota2 Senate — 649 (Game simulation)
 So we have two queues, so to tackle the rounds we simply add n to the elements’ index if they voting in the next round 


The General Rule for Comparators
The comparator (a, b) -> result 
 always follows one rule, no exceptions:
result is negative  →  a comes first (a has higher priority)
result is zero      →  doesn't matter
result is positive  →  b comes first (b has higher priority)


Question 158. Kth Largest Element in an Array — 215 (Quickselect/heap)
Simply use a min heap, add to heap then if size is greater than k , simply remove the top/smallest element and replace it with the new number and when we do this continuously at the end the top of the heap is the Kth largest element as 
Question 159. Kth Largest Element in a Stream — 703 (Running kth)
Same min heap concept but for implementation based questions remember this 
Staring at a blank class?
│
├── What does the problem ask me to track over time?
│ └── Those are your FIELDS, or what does your methods needs from outside
│
├── What am I given at the start?
│ └── Those are your CONSTRUCTOR parameters
│
├── What operations must work?
│ └── Those are your METHODS
│
└── What must each method return?
└── Read the return type in the signature

Question 160. Last Stone Weight — 1046 (Heap simulation)
Offer everything/all elements in a maxheap then just poll the bigger ones, subtract and offer again and at the last return heap.peek();
Question 161. K Closest Points to Origin — 973 (Distance heap)
Comparator use it is 
PriorityQueue<int[]> heap = 
new PriorityQueue<>( (a, b) -> (a[0]*a[0] + a[1]*a[1]) - (b[0]*b[0] + b[1]*b[1]) );
The comparator (a, b) -> result 
 always follows one rule, no exceptions:
result is negative  →  a comes first (a has higher priority)
result is zero      →  doesn't matter
result is positive  →  b comes first (b has higher priority)

Question 162. Top K Frequent Elements — 347 (Frequency heap)
Heap takes ologn
So we use bucket sort 
Create a hashmap, then create an arraylist with vaues/frequency as index then create an int[] result to store, stuff to remember in this code is 
if(bucket[f]==null) bucket[f]=new ArrayList<>();
Also how to tackle the filling of the array 




Question 163. Sort Characters By Frequency — 451 (Frequency sort)
Same thing use bucket sort 
Have a frequency as index 
Question 164. Find K Pairs with Smallest Sums — 373 (Pair generation) next 4 mushkil hai

Question 165. Reorganize String — 767 (Greedy scheduling)

Question 166. Task Scheduler — 621 (CPU scheduling)
So the problem does gets a little complex as we need only to keep track of the count, so step1) make an frequency array 
step2)offer everything to heap 
step3)so the main logic is we use cycles of n+1, and we take the most common  one first using the maxheap
Then we store the polls in a temp and while
Question 167. Meeting Rooms II — 253 (Concurrent tracking)
	Locked



## Trees - Traversal & Basics (168-192)
	All the iterative dfs uses stack to implement and bfs uses a queue
Question 168. Binary Tree Inorder Traversal — 94 (Iterative DFS)
So simply first we go as possible left as possible while adding to stackthen we simply pop the stack, add to result and then simply curr=curr.right
Question 169. Binary Tree Preorder Traversal — 144 (Iterative DFS)
	Here we have a stack we simly first push the root, and then curr=stack.pop
And then we push curr.right first and then curr.left in the stack and then we mutually automatically while iterations add top of stack to the result.
Question 170. Binary Tree Postorder Traversal — 145 (Iterative DFS)
Same have the stack push root to it, then the left , then right and build the result adn then at the very end just reverse the whole
Question 171. Binary Tree Level Order Traversal — 102 (BFS basics)
So we need to give answer level wise, so weuse a queue andhere in the queue, the size of the level is determined by the size of the queue, and then we run a for loop inside the a while loop(until queue is not empty) , and then we  simply add the top of the queue and offer left adn then right 
Question 172. Binary Tree Level Order Traversal II — 107 (Bottom-up BFS)
	Just reverse






Question 173. Binary Tree Zigzag Level Order Traversal — 103 (Alternating BFS)
So we similarly prepare the leves, have a level an arraylist Do the same thing like previous question and only reverse the result, alternatiely using collections to alternate it at each level 
Question 174. Binary Tree Right Side View — 199 (Level selection)
So we use bfs and only add to result when on last node rest is same 
Question 175. Average of Levels in Binary Tree — 637 (Level computation)
We similarly create each level and then at the end get the average divide by size, and then just add to result

Question 176. Find Bottom Left Tree Value — 513 (Level tracking)
You use the same bfs and then and each time i==0 you update result and then at the very end just return result
Question 177. Maximum Depth of Binary Tree — 104 (Recursive DFS)
We can use bfs , well technically its o(n) but a dfshaas a bettersolution 
Recursive solution 
Simply return Math.max(maxDepth(root.left),maxDept(root.right))+1;



Question 178. Minimum Depth of Binary Tree — 111 (BFS or DFS)
Use bfs ,follow same template and then in middle if a node/curr is 
if(curr.left==null && curr.right==null) return depth;

Question 179. Same Tree — 100 (Structure comparison)
As simple bfs , dfstraversal dosnt work because in answer we dont include nulls, one approach couldbe maybe include the nulls too but its tough include checks, so we instead compare each and every node  during traversal , and it must follow three things
First if both exists, if their values match, and if its same for left and right subtrees
Question 180. Symmetric Tree — 101 (Mirror validation)
So a mirror/symmetric tree,the outmost branches shold be equal and th inner most branches should be equal , so we can either do this recursively or use a queue
Question 181. Invert Binary Tree — 226 (Structure modification)
So according to recursion what we do is have temp treenode and equal it to root.left and then just root.left=root.right and then root.right=temp
And then simple recursively inverttree(root.left) and then inverttree(root.right)

For iterative method use bfs 
Use queue, and change the left and right and then offer left nd right
Question 182. Balanced Binary Tree — 110 (Height checking)
So we have the height helper function, and then in the main we simply find the right and left heights and then simply return 

Math.abs(left-right)<=1 && isBalanced(root.left) && isBalanced(root.right);







Question 183. Diameter of Binary Tree — 543 (Path through root)
So the diameter is the max length between right and left , so what we do is have a height function and then we simply calculate left and right and return height.
Question 184. Sum of Left Leaves — 404 (Conditional sum)
Recursion can be done in many ways but what we can do is find left leafs for each node 
if(root.left!=null && root.left.left==null && root.left.right==null) sum+=root.left.val;

Question 185. Leaf-Similar Trees — 872 (Leaf sequences)
Collect node amd compare, either recursion or dfs using a stack 
Question 186. Path Sum — 112 (Root-to-leaf DFS)
Think in a recursion , what would be base cases , when null ,when we come across leaf nodes,and then in recursion we simply check for left and right subtrees while subtracting the value from targetsum
BASECASE1  if(root==null) return false;
BASECASE2 if(root.left==null && root.right==null) return root.val==targetSum;
return hasPathSum(root.left,targetSum-root.val) ||  hasPathSum(root.right,targetSum-root.val);

Question 187. Path Sum II — 113 (All paths backtracking)
Simple backtracking, you visit everything and (base case) the leaf node youcheckif the targetsum is equal to current sum and if yes simply add. Do same for left and right subtrees and at end backtrack by path.remove(path.size()-1);
Removing the most recent node



Question 188. Binary Tree Paths — 257 (Path strings)
We also need to realize most questions can be solved multiple ways, this can be done both using a backtrack or without it.WHi concept hai root tak jao, base cases socho aur lgao fir recursion end me
Question 189. Sum Root to Leaf Numbers — 129 (Number formation)
But my man , backtracking is always better, simply have a helper function to convert an arraylist to number, backtrack and for each one if its a leaf node u simply add sumto it. And then simply do left and right tress, at the ended we simply bactrack, by removing the last element i.e.nums.remove(nums.size()-1);
Question 190. Subtree of Another Tree — 572 (Subtree matching)
You can have a string, usme dfs fir check if substr presentin str,but its notoptimal 
Optimal is using issamtree helper methos and then check if subroot issmaetree for right and left child subtrees
Question 191. Merge Two Binary Trees — 617 (Tree merging)
Simply think of cases,either both roots are null, either one isnull then return the other one.then root1.val+=root2.val; and then just fill root1’s left and right

Question 192. Count Good Nodes in Binary Tree — 1448 (Path tracking)
Simply 
if(node.val>=maxsofar){
           count=1;
Aur fir count+=dfs(on right tree and left subtrees)


## BST Operations (193-207)
Question 193. Search in a Binary Search Tree — 700 (BST search)
What changed only that its a binary tree,base cases socho ,  root==null;
root.val==val;
root.val>val search left side else search right side 
Question 194. Insert into a Binary Search Tree — 701 (BST insertion)
We only inswert when node ia null and what we check is the root compared to vlue ;if smaller to left ,else to right 
Question 195. Delete Node in a BST — 450 (BST deletion)
Searching is same else if(key>root.val)
Then the thign is deleting, while deleting three case, 
if node is leaf no problem , return null, 
If node has one child (a)if left is null return right (b) else return left
If node has 2 children, we find the smallest in the right subtree, because 
when deleting a node with 2 children, we need a replacement that keeps the BST valid. Inorder Successor = smallest value in RIGHT subtree = the value just ABOVE the deleted node in sorted order 
It's safe because: 
- it's LARGER than everything in left subtree 
- it's SMALLER than everything in right subtree 
- BST property maintained! 
Question 196. Validate Binary Search Tree — 98 (BST property)
Have a helper function and just for a minimum and maximum check if its always in range and then for the left binary tree only constrain the upper value 
And then in right bin ary tree constrain the lower value/min 
private boolean isvalid(TreeNode root, long min, long max){
       if(root==null) return true;
       if(root.val<=min || root.val>=max) return false;
       //next line simply means if its in range
       return isvalid(root.left,min,root.val) && isvalid(root.right,root.val,max);
   }
   public boolean isValidBST(TreeNode root){
       return isvalid(root,Long.MIN_VALUE,Long.MAX_VALUE);
   }

Question 197. Kth Smallest Element in a BST — 230 (Inorder + count)
Phle boht galat soch lye the, jod tod kr rhe the 
But instead we use inorder left, root, right, according to dfs gives a sorted lsit 



Question 198. Minimum Absolute Difference in BST — 530 (Inorder difference)
Simply inorder chalte, aur difference lete jate, aur mindiff global variable , and then we also use a prev for the difference and calculate min diff in indeed and then just continue it 

Question 199. Find Mode in Binary Search Tree — 501 (Frequency tracking)
So the idea is that the inorder gives a sorted list in a bst so the same elements will be adjacent and you keep track of the most occurred one till now 
Last me list se array use return result.stream().mapToInt(i -> i).toArray();
Also remember that we need to clear the list too when we find a new maxstrk 
Question 200. Lowest Common Ancestor of a BST — 235 (BST LCA)
So what happens here is if p and q both less than the root , then we recurse left tree, if bigger then right tree and when none of these then root is the LCA
Question 201. Convert Sorted Array to Binary Search Tree — 108 (Balanced construction)
Have a helper function , that takes nums, l=0; r=len-1; 
So we always make mid as root, and then left side from l to mid-1 becomes left subtree and mid+1 to right becomes right subtree
Question 202. Trim a Binary Search Tree — 669 (Range pruning)
Simple to trim if node.val<low to discard node and leftsubtree and return trim(rightbst)
And if node.val>high , discard node and right subtree return trim(leftsubtree)
Aur fir root.left aur root.right ko simply attach by root.left=trimBST(root.left,low,high);




Question 203. Range Sum of BST — 938 (Conditional sum)

Question 204. Increasing Order Search Tree — 897 (Flatten BST)
Question 205. Convert BST to Greater Tree — 538 (Reverse inorder)
Question 206. Two Sum IV - Input is a BST — 653 (BST search)
Question 207. Lowest Common Ancestor of a Binary Tree — 236 (General tree LCA)

