class Solution:
    def pivotIndex(self, nums: List[int]) -> int:
        ls=0
        rs=0
        total=sum(nums)
        for i in range(len(nums)):
            rs=total - ls -nums[i]
            if(rs==ls):
                return i
            ls+=nums[i]
        return -1