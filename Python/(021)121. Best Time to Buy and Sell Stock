class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        minprice = 2**31 - 1
        maxpro = 0
        for price in prices:
            if minprice > price:
                minprice = price
            pro = price - minprice
            if maxpro < pro:
                maxpro = pro
        return maxpro