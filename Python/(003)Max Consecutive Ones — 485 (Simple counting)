class Solution:
    def findMaxConsecutiveOnes(self, nums: List[int]) -> int:
        curr=0
        maxx=0
        for i in range(len(nums)):
            if(nums[i]!=1):
                maxx=max(maxx,curr)
                curr=0
            else:
                curr+=1
        return max(maxx,curr)